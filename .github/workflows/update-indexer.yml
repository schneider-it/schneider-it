name: Update indexer.json file

on:
  push:
    branches:
    - main

jobs:
  update-indexer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd tools/indexer/
      - run: python3 -m venv indexer-venv
      - run: source indexer-venv/bin/activate
      - run: python3 indexer.py
